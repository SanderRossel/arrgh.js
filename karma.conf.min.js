module.exports = function(config) {
    config.set({
        frameworks: ['jasmine'],
        files: [
            'dist/*.min.js',
            'test/spec/*.js'
        ],
        reporters: ['progress', 'junit'],
        port: 9877,
        autoWatch: true,
        browsers: ['Chrome', 'IE', 'Firefox'],
        singleRun: true,
        junitReporter: {
            outputDir: 'test/junit.min',
            suite: 'arrgh.min.js',
            useBrowserName: true
        }
    });
};